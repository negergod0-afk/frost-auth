package com.zenya.module.modules.misc;

import com.zenya.module.Category;
import com.zenya.module.Module;

public final class AutoFireworks extends Module {
    public AutoFireworks() {
        super("Auto Fireworks", Category.MISC);
        setDescription("Automatically uses fireworks while flying with an elytra.");
    }
}
