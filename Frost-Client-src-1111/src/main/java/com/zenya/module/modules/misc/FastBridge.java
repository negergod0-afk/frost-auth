package com.zenya.module.modules.misc;

import com.zenya.module.Category;
import com.zenya.module.Module;

public final class FastBridge extends Module {
    public FastBridge() {
        super("Fast Bridge", Category.MISC);
        setDescription("Allows you to bridge faster by automatically sneaking at block edges.");
    }
}
